package com.floragunn.searchsupport.jobs.core;

import org.hamcrest.MatcherAssert;
import org.hamcrest.Matchers;

public class IndexJobStateStoreTest {
    // TODO implement test related to watch creation on startup
    // instances should be created when method com.floragunn.searchsupport.jobs.core.IndexJobStateStore.initialize is invoked
    // and when method com.floragunn.searchsupport.jobs.core.IndexJobStateStore.clusterConfigChanged is executed
}